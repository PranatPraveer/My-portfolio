// This function scrolls the page to the top.
function scrollToTop() {
  window.scrollTo(0, 0);
}

// This function is called when the user clicks on the "Contact" link.
function contact() {
  window.location.href = "mailto:bard@example.com";
}
